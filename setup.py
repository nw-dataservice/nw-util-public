from setuptools import setup, find_packages

setup(
    name='nw_util_public',
    version='0.1',
    packages=find_packages(),
    description='A simple utility module',
    author='Patrick Ng',
    author_email='patrick.ng@networld.hk',
    url='https://github.com/yourusername/my_module',  # Update with your URL
    install_requires=[
        # Add any dependencies here
    ],
)